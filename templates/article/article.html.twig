{% extends 'base.html.twig' %}

{% block body %}
    {% for art in article %}
    <a href="#" class="category-link">{{ art.categoryTitle }}</a>
    <a href="#" class="post-link">
        <h1>{{ art.title }}</h1>
    </a>
    <time>{{ art.publicationDate | date('d-m-Y') }}</time>
    <p>{{ art.shortDescription }}</p>
    {% endfor %}
{%  endblock %}